version: '2'
services:
  jenkins:
    container_name: jenkins
    image: jenkins:2.46.1
    ports:
      - "9393:8080"
    restart: always
    volumes:
      - ./var/jenkins:/var/jenkins_home
      - ./var/android/sdk:/var/android/sdk
      - ./var/android/ndk:/var/android/ndk